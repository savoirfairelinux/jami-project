#!/usr/bin/make -f
# -*- makefile -*-

# export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_configure:
	mkdir build && cd build
	../configure \
		-opensource \
		-confirm-license \
		-nomake examples \
		-nomake tests \
		-prefix /opt/qt-jami
